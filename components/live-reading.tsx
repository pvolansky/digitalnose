'use client';
import {useEffect,useState} from 'react';
import type {Reading} from '@/lib/domain/types';
export function LiveReading({reading,initialNow,demo=false}:{reading:Reading|null;initialNow:number;demo?:boolean}){const [now,setNow]=useState(initialNow);useEffect(()=>{const timer=setInterval(()=>setNow(Date.now()),15000);return ()=>clearInterval(timer)},[]);const age=reading?Math.max(0,Math.floor((now-Date.parse(reading.minute_start_utc))/60000)):null;const stale=age!==null&&age>3;return <section className="panel"><div className="row spread"><p className="eyebrow" style={{color:stale?'#946516':'#21664b'}}>● {demo?'Demo readings':reading?stale?'Awaiting fresh data':'Live':'Not connected'}</p><span className="muted" style={{fontSize:13}}>{age===null?'No readings yet':`Latest minute · ${age<1?'just now':age<60?`${age} min ago`:age<1440?`${Math.floor(age/60)} hours ago`:`${Math.floor(age/1440)} days ago`}`}</span></div><div className="metrics">{[['TVOC',reading?.tvoc_mean,'ppb'],['eCO₂',reading?.eco2_mean,'ppm'],['AQI',reading?.aqi_max,'/ 5']].map(([label,value,unit])=><div key={label}><p className="muted" style={{fontSize:14}}>{label}</p><div><span className="metric-value">{value==null?'—':Number(value).toLocaleString('en-GB',{maximumFractionDigits:1})}</span><span className="muted" style={{marginLeft:10,fontSize:15}}>{unit}</span></div></div>)}</div><p className="muted" style={{fontSize:12,marginTop:24}}>ENS160 estimates · eCO₂ is an equivalent CO₂ estimate, not a direct CO₂ measurement.</p></section>}
